pipeline {
  agent any
  stages {
    stage('Checkout code') {
      steps {
        git(url: 'https://github.com/rajeshbreddy/http', branch: 'dev')
      }
    }

    stage('') {
      steps {
        sh 'ls  -la'
      }
    }

  }
}